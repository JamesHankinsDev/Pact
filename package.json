{
  "name": "pact",
  "version": "0.1.0",
  "private": true,
  "description": "A community fitness platform for small groups (2–6 people).",
  "scripts": {
    "dev": "pnpm --filter @pact/web dev",
    "build": "pnpm --filter @pact/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
